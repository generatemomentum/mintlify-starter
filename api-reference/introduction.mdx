---
title: 'Introduction'
description: 'Identity research API reference'
---

## Welcome

<Card
  title="Identity Research API"
  icon="magnifying-glass"
  href="https://github.com/mintlify/starter/blob/main/api-reference/openapi.json"
>
  View the OpenAPI specification file
</Card>

## Authentication

All API endpoints are authenticated using an API key sent in the `X-API-Key` header.

## Job workflow

All operations run asynchronously. Each one returns a `job_id` you poll until the result is ready.

1. **Start** — call the operation endpoint (e.g. [Research identity](/api-reference/endpoint/research-identity)) with its required fields.
2. **Poll** — call [Poll job status](/api-reference/endpoint/get-job) with the `job_id`. Respect the `Retry-After` header between requests.
3. **Read result** — when `status` is `completed`, the typed `result` is included. Its shape depends on the job `type` — see the operation's **Completed result** page for the schema.

While in progress, `status` will be `pending` or `processing`. If processing fails, `status` will be `failed`.
