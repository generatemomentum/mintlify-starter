---
title: 'Completed result'
openapi-schema: 'ResearchIdentityResult'
---

This object is returned in `result` when you poll `GET /v1/jobs/{job_id}` and `status` is `completed` for jobs created by `POST /v1/research-identity`.
