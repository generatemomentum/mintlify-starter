---
title: 'Completed result'
openapi-schema: 'ResearchIdentityResult'
---

This object is returned in `result` when you poll `GET /jobs/{job_id}` and `status` is `completed` for jobs created by `POST /research-identity`.
